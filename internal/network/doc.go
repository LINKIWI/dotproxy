// Package network contains abstractions for communicating with other machines over the network. It
// attempts to abstract away the API differences between TCP and UDP connections, and minimizes the
// exposed interaction surfaces for TLS connections in an effort to simplify client usage.
package network
