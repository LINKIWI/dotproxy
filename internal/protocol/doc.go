// Package protocol concerns itself primarily with DNS protocol-specific business logic. It contains
// logic used for understanding the DNS protocol and mediating requests and responses between the
// client and upstream server(s).
package protocol
